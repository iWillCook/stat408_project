---
title: "A demonstration of my code"
format: html
---

```{r}
library(tidyverse)
library(nycflights13)
```

```{r}
flights |> 
  filter(arr_delay >= 120 & dep_delay == 0) |>
  filter(carrier %in% c("UA", "AA", "DL"))
```

